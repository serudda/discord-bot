declare module '@discord-bot/eslint-config' {
  const config: import('eslint').Linter.Config[];
  export default config;
}

declare module '@discord-bot/eslint-config/astro' {
  const config: import('eslint').Linter.Config[];
  export default config;
}
