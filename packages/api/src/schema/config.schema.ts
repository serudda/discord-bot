import { z, type TypeOf } from 'zod';

/*------------------------------------*/

export const getConfigInput = z.object({});
export type GetConfigInputType = TypeOf<typeof getConfigInput>;

/*------------------------------------*/
